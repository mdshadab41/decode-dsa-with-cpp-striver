#include <bits/stdc++.h>
using namespace std;

int main()
{
    queue<int> que; // FIFO

    que.push(1);
    que.push(2);
    que.push(3);
    que.push(3);
    que.push(5);
    que.push(7);
    que.push(8);

    que.back();

    que.front();

    que.back();

    // rest same as stack
}